version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: dockerise
    task:
      jobs:
        - name: Generate docker image
          commands:
            - checkout
  - name: Unit test
    task:
      jobs:
        - name: Test Extract
          commands:
            - python -m unittest tests.test_extract
