version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: dockerise
    task:
      jobs:
        - name: Generate docker image
          commands:
            - checkout
after_pipeline:
  task:
    jobs:
      - name: Unit Test
        commands:
          - python -m unittest -v tests/test_extract.py
